# secrets — never commit
.env

# python
.venv/
__pycache__/
*.pyc

# runtime state / local artifacts
data/*.db
data/online_state.json
# research traces: runs can be large, keep the mock fixture only
data/traces/*
!data/traces/fixture-mock/
!data/traces/fixture-fanout/
nohup.out
*.log

# editors
.vscode/
.idea/

# internal-only brief — never publish
SV-Serve_assumptions_and_instructions.pdf
SV-Serve_Required_Changes_and_Untouched_Areas.docx
*.pdf
*.docx

# local working files
image*.png
logo.png
Screenshot*.png
data/mtbench_gpt5.json

# research paper — local only, never push
paper/

# Claude session memory — local only
CLAUDE_MEMORY.md

# ...but the fixture run's collection log is checkpoint evidence (beats *.log above)
!data/traces/fixture-mock/collect.log
!data/traces/fixture-fanout/collect.log
